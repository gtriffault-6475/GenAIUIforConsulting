# Hard-fails `npm install` on the wrong Node version instead of only
# warning — `package.json`'s `engines.node` is otherwise advisory only.
# See README.md "Getting started" for why this matters (`node:sqlite`'s
# `stmt.setReturnArrays` only exists from Node 24+).
engine-strict=true
