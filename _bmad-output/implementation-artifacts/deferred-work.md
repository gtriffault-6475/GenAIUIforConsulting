- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-panneau-skills.md`
  summary: `actions/skill.ts`'s `seedFixturesIfEmpty` only checks whether a project has zero `PROJECT_SKILL` rows to decide whether to seed — the same structural gap already documented for `actions/conversation.ts`'s equivalent seeder (see the Story 2.2 entry below). If even one `PROJECT_SKILL` row already exists for a project (from any future write path), the seeder skips entirely and never adds the other missing fixture skill.
  evidence: Flagged by the edge-case review pass during Story 2.4's retroactive review gate, as the mirror image of an already-filed Story 2.2 finding that wasn't re-filed here. Currently inert: round 1 has no code path that writes a single `PROJECT_SKILL` row outside this seeder, since no "add skill" mechanism exists yet (OQ-6 deferred beyond this epic). Revisit only if/when a real add-skill mechanism is built.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffolding-du-projet-et-fondations-partagees.md`
  summary: Two DESIGN.md color tokens fail WCAG AA contrast (`text-muted` #8a90a3 on `background` #f3f4f8 ≈ 2.9:1; `ai-accent-foreground` #ffffff on `ai-accent` #7c5cfc ≈ 4.4:1, both below the 4.5:1 normal-text threshold).
  evidence: Verified by direct contrast computation during Story 1.1 review. Root cause is the token values chosen upstream in DESIGN.md (an earlier UX-phase artifact), which this story's frozen Boundaries require matching exactly — not something this story's implementation can unilaterally change. Revisit by adjusting the two DESIGN.md tokens (or their usage) and propagating to app/globals.css.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffolding-du-projet-et-fondations-partagees.md`
  summary: No `.env.local.example` documents the `ANTHROPIC_API_KEY` environment variable a future developer will need.
  evidence: This story's frozen Boundaries explicitly exclude `@anthropic-ai/sdk` wiring ("Epic 2's concern") — the env var has no consumer yet, so there's nothing to validate an example against. Add `.env.local.example` (and a README mention) when Epic 2 / Story 2.5 first reads `ANTHROPIC_API_KEY`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffolding-du-projet-et-fondations-partagees.md`
  summary: `components/OverlayProvider.tsx`'s single `openOverlayId` string has no collision guard if two independent call sites ever pass the same id.
  evidence: No consumer exists yet in this story's diff to demonstrate a real collision (maybe-false, would be medium if it occurred). Settle once Epic 2 (skill-add entry point) and Epic 4 (suggestion retravail field) add real overlay ids — either confirm ids are naturally unique per feature, or namespace them (e.g. `skill-add`, `suggestion-retravail-{suggestionId}`).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-selection-d-un-projet-octopod.md`
  summary: There is no UI affordance to switch to a different active project once one is selected — the only way back to the selector is deleting `db/local.db`.
  evidence: A real gap a consultant would hit in daily use, but excluded by this story's frozen Intent (only describes a selector when none is active and a top bar once one is) and by the epic's "un seul projet actif à la fois" framing — no switcher UX is specified anywhere in round 1's planning artifacts. Revisit once a story defines the intended UX for changing projects (e.g. a click target on the top bar reopening the selector, with a decision on what happens to any active conversation/livrable state).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-panneau-contexte.md`
  summary: `actions/document.ts`'s sync upsert (`onConflictDoUpdate` keyed on `document.id`) never refreshes `projectId`, and `document.id` is a global (not per-project) primary key.
  evidence: Not triggered today — the mock's seed ids are namespaced per project (`doc-acme-*`, `doc-audit-*`), so no collision occurs across the two seed projects (independently confirmed by two review passes). Would only manifest if a future real `DriveProvider` or Story 1.4's manual-add path ever produced an `id` colliding across two different projects: the row's content would silently overwrite while `projectId` stayed pointed at the original owner. Revisit if/when a real (non-mock) `DriveProvider` is wired in, or when Story 1.4 defines how manual-document ids are generated.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-panneau-contexte.md`
  summary: The drive sync in `actions/document.ts` only inserts/updates `source: 'drive'` rows — it never deletes a row whose id disappeared from the provider's latest listing.
  evidence: Not exercisable today since the mock's seed list is static (always returns the same ids). Once a real `DriveProvider` exists, a file actually removed from Octopod's drive would remain listed in the Contexte panel forever. Revisit when a real (non-mock) `DriveProvider` replaces `integrations/mock/drive-provider.ts`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-panneau-contexte.md`
  summary: `ContextPanel`'s folder grouping treats `folderPath: null` (root) and `folderPath: ''` (empty string) as two distinct, equally headerless groups.
  evidence: Not triggered today — the mock adapter only ever emits `null` for root-level documents, never `''`. A future real `DriveProvider` that uses `''` as its "no folder" convention would silently split root documents into two indistinguishable headerless clusters. Revisit alongside the real `DriveProvider` work; likely fix is normalizing `''` to `null` at the port boundary.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-panneau-contexte.md`
  summary: `app/page.tsx` has no `Suspense`/`loading.tsx` boundary — the whole page (including the top bar) blocks on `listDocuments` resolving, not just the Contexte panel.
  evidence: Pre-existing pattern from Story 1.2 (already true for `getActiveProject`), not a regression introduced here. Harmless today since the mock adapter's simulated latency is short, but would show as a blank page rather than a progressively-rendering one if a real, slower `DriveProvider`/`ProjectProvider` were wired in. Revisit if real integrations introduce noticeable latency.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-panneau-contexte.md`
  summary: The `SELECT` in `actions/document.ts` that reads back `document` rows has no `ORDER BY`, so within-folder document order relies on incidental SQLite row order rather than a guaranteed contract.
  evidence: Low risk (order is stable in practice today), but a future SQLite/driver change could reorder documents within a folder with no code change on our side. Revisit by adding an explicit `.orderBy(document.name)` (or similar) if this is ever observed to matter.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-ajout-d-un-document-hors-drive.md`
  summary: If a consultant closes the add-document form (Escape or click-outside) in the brief window between clicking "Ajouter" and the Server Action resolving, the request still completes: on success the document is silently added anyway (the user believed they cancelled it); on failure, `setSubmitError` fires on an already-unmounted `AddDocumentForm`, so the error message never displays.
  evidence: Root cause is architectural: `{isOpen && <AddDocumentForm .../>}` in `components/ContextPanel.tsx` unmounts the form (and its in-flight-submission state) the instant the shared `OverlayProvider` closes, unlike `ProjectSelector.tsx` where the equivalent `error`/`isPending` state lives in the always-mounted parent, not the conditionally-rendered dropdown content. Two independent review passes flagged this; not fixed now because a fully correct fix (keeping the form's request alive and re-surfacing an error after an out-of-band close) requires lifting submission state into `ContextPanel` and deciding how a post-close error gets acknowledged — non-trivial for a very narrow timing window on a local, single-user, synchronous SQLite write. Revisit if this is ever actually observed happening (unlikely: the write is a same-machine, near-instant round trip) or when a future story needs the same overlay-form pattern elsewhere and the fix can be made once, generically.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-ajout-d-un-document-hors-drive.md`
  summary: The "Dossier" field in the add-document form is freeform text matched by exact string against drive folder names (`components/ContextPanel.tsx`'s `groupByFolder`) — a consultant typing `"Comptes rendus"` instead of the drive's `"Comptes-rendus"` creates a second, visually indistinguishable group instead of joining the existing one.
  evidence: Not a regression (the grouping logic is Story 1.3's, untouched here), but Story 1.4 is the first thing that lets a human type a folder name freehand instead of it coming from seeded/mock data. Revisit if this causes real confusion — options include autocomplete against existing folder names, or normalizing (case/whitespace-insensitive) matching in `groupByFolder`.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-panneau-mattermost.md`
  summary: `actions/mattermost.ts`'s `getLastMattermostMessage` passes `channelRef` straight to the provider with no truthiness check; an empty-string `mattermostChannelRef` would silently render the same "Aucun message pour ce canal." state as a channel that genuinely has no traffic — the two very different root causes (broken reference vs. quiet channel) are indistinguishable in the UI. Relatedly, `MattermostPanel`'s timestamp formatting (`new Date(postedAt)...`) has no validation against a malformed ISO string — not a crash (`"Invalid Date"` renders, doesn't throw), but an unguarded implicit assumption.
  evidence: Neither is triggered today: `PROJECT.mattermostChannelRef` is DB `NOT NULL` and both seed projects carry valid, non-empty refs with valid ISO timestamps in the mock — there is no path in round 1 that produces either an empty ref or a malformed timestamp. Revisit only if a future real `MattermostProvider` (or a corrupted `PROJECT` row) can actually produce either case.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-conversations-multiples-et-selection-active.md`
  summary: **Update (Epic 2 retrospective, 2026-09-16) — half resolved, half still open.** Originally: both `message` and `conversation` reads had no `ORDER BY`. Story 2.5 resolved the `message` half by adding `message.createdAt` and `.orderBy(message.createdAt)` to `getActiveConversation`'s history read. `listConversations` (the left-sidebar conversation list) still has no `ORDER BY` — its Story 2.2 triage note dismissed this as "pré-existant... no action" without re-examining the premise, even though Story 2.2 itself (`createConversation`) is a second, user-triggered insert path that already invalidates "fixture seeding is the sole inserter" for `conversation` specifically.
  evidence: Confirmed by three independent review passes across two epics (adversarial/edge-case in Epic 1's retro precursor to this entry; adversarial again in Epic 2's retrospective, which traced the premise drift). Live-verified during the Epic 2 retrospective that a newly created conversation still appends at the list's end (array-return order matches insertion order today), so no user-visible defect exists yet — but the guarantee remains an unenforced SQLite implementation detail. Revisit by adding `.orderBy(conversation.???)` — `conversation` has no timestamp column today, so this needs either a `createdAt` column (mirroring `message`'s fix) or accepting insertion order as good enough for round 1's fixture-plus-manual-creation volume.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-confidentialite-de-la-conversation.md`
  summary: The FR-10 privacy boundary (conversation content must never leak outside its own view) is enforced only by code comments — no test, lint rule, or type-level guard would catch a future PR that widens `ConversationSummary` with a content field or threads `activeConversationResult`/`MessageSummary[]` to an unrelated component.
  evidence: Flagged by the adversarial and edge-case review passes. Genuinely the cheapest option available today: this repo has no test suite, no ESLint config, and no CI at all (confirmed by `find . -iname "*.test.ts*"` returning nothing) — architecturally out of scope for round 1 per ARCHITECTURE-SPINE.md's own Deferred section ("Tests automatisés, CI — non abordés"). Revisit only if/when this project adopts a test or lint setup; at that point a lint rule or a stronger type (e.g. a branded/opaque type preventing `ConversationSummary` from ever being assignable from something carrying `.content`) becomes a cheap addition.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-confidentialite-de-la-conversation.md`
  summary: **Resolved (Epic 2 retrospective, 2026-09-16)**, the promised revisit. `sendMessage`'s two catch blocks (`actions/conversation.ts`) — the only new error paths this epic added that touch real, user-authored message content — log `console.error('<message>', error)` where `error` is either a Drizzle/`node:sqlite` exception or an `@anthropic-ai/sdk` error. Confirmed: `node:sqlite` constraint/FK errors report the constraint name and table, never the bound parameter values (same behavior already relied on for the original entry); the SDK's errors describe HTTP/API failure shapes, never echo request body content. Neither path can interpolate a user's message text into a log line.
  evidence: Direct read of `actions/conversation.ts`'s two `sendMessage` catch blocks plus the same `node:sqlite` non-echoing behavior confirmed for the original (pre-Story-2.5) entry. No code change needed; closing this item.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-creation-d-une-nouvelle-conversation.md`
  summary: `actions/conversation.ts`'s `seedFixturesIfEmpty` only checks whether a project has zero `CONVERSATION` rows to decide whether to seed the Story 2.1 fixtures — it cannot distinguish "never seeded" from "a real conversation was created first." A direct/programmatic call to `createConversation` on a never-seeded project would permanently suppress that project's fixtures.
  evidence: Flagged by the edge-case review pass. Not reachable through the shipped UI: `app/page.tsx` always calls `listConversations`/`getActiveConversation` (both of which seed synchronously) before any render that could expose the "Nouvelle conversation" button, so the button cannot fire before seeding has already happened for that project. Only reachable via a script or future code path that calls `createConversation` directly, bypassing the page's own render flow. Revisit only if such a path is ever added.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-conversations-multiples-et-selection-active.md`
  summary: `app/page.tsx`'s active-conversation refresh has a narrow desync window — if `selectConversation` (the write) succeeds but the immediately following `getActiveConversation` (the read, on `router.refresh()`) independently fails, the left sidebar shows no row as active even though `PROJECT.activeConversationId` is correctly set in the DB.
  evidence: Flagged by the edge-case review pass. Both calls read the same local, synchronous SQLite DB, so this needs a genuine intermittent failure between two back-to-back reads on one process to trigger — no such failure mode exists in round 1's mock/local setup. Revisit only if a future non-local DB (or real network-backed persistence) makes independent read/write failures plausible.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-selection-du-modele-et-envoi-d-un-message.md`
  summary: `actions/skill.ts`'s `listLoadedSkillInstructions` reads `project_skill` with no `ORDER BY`, so the skills concatenated into the system prompt have no guaranteed order — contradicting AD-11's "load order" claim (`skills/buildRequest.ts`'s own header comment repeats this).
  evidence: Flagged by the Epic 2 retrospective's adversarial pass. Harder to fix than the equivalent `message`/`conversation` ordering gaps: `project_skill` has no PK and no candidate ordering column at all (by design — see its schema comment, "only the join... no standalone id/PK"). Not triggered visibly today (round 1 seeds at most 2 skills per project, and prompt-order rarely changes an answer's substance for 2 short instruction blocks), but would need an explicit ordering column (e.g. `loadedAt`) added via migration to actually guarantee load order. Revisit if a project ever has 3+ loaded skills where instruction order plausibly matters, or when a real skill-loading mechanism (currently deferred, PRD OQ-6) is built.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-conversations-multiples-et-selection-active.md`
  summary: `actions/conversation.ts` has grown to 458 lines / 5 exported Server Actions (`listConversations`, `getActiveConversation`, `selectConversation`, `createConversation`, `sendMessage`) across Stories 2.1/2.2/2.5, while every sibling entity in this codebase gets its own dedicated ~100-160 line file. `sendMessage` specifically also orchestrates cross-entity logic (calling into `actions/skill.ts` and `skills/buildRequest.ts`) that no other `actions/*.ts` file does.
  evidence: Flagged by the Epic 2 retrospective's adversarial pass (aggregate "god-file" view). Not a functional defect — AD-2 (only this file touches CONVERSATION/MESSAGE) still holds — but the file has taken on a materially different, second responsibility (the only cross-entity orchestration point in the app) than "CRUD for two tables." Revisit by splitting `sendMessage` (and its orchestration) into its own `actions/message.ts` if this file keeps growing — likely once Epic 4 adds tool-using skills per `ARCHITECTURE-SPINE.md`'s roadmap.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-panneau-livrables.md`
  summary: The `seedFixturesIfEmpty` idiom (a synchronous `db.transaction` check-then-insert, whose correctness depends on the invariant "no `await` inside the callback") is now independently copy-pasted in `actions/conversation.ts`, `actions/skill.ts`, and `actions/livrable.ts` — the exact class of duplication Epic 1's own retrospective flagged and had fixed once (`ActionResult<T>`, `withLatency<T>`) with an explicit note to extract shared idioms "avant qu'Epic 2 n'ajoute une 4e copie."
  evidence: Flagged by the Epic 2 retrospective's adversarial pass; the `ActionResult<T>` lesson was correctly applied (consolidated into `actions/types.ts`), but this newer, more concurrency-sensitive idiom was not. Each copy currently explains the "no `await`" invariant in its own prose comment rather than encoding it once. Revisit by extracting a shared `seedIfEmpty<T>(table, projectId, seedFn)` helper (e.g. `db/seed-helpers.ts`) before a 4th copy appears — plausibly Epic 3 or 4.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-selection-du-modele-et-envoi-d-un-message.md`
  summary: `message.createdAt`'s migration-level `NOT NULL DEFAULT '1970-01-01T00:00:00.000Z'` is a real, reachable value for any future insert path that forgets to pass `createdAt` explicitly, not just a one-time backfill artifact for pre-existing rows — nothing at the application level enforces that every inserter supplies it.
  evidence: Flagged by the Epic 2 retrospective's adversarial pass. Today `sendMessage` and the Story 2.1 fixture seeder both correctly stamp `createdAt` explicitly, so this is not currently triggered — but a future insert path (plausibly Epic 4's `propose_livrable_content` tool or another agent-driven writer) that omits it would silently produce a message sorted to the very start of its conversation's history, with no error. Revisit by adding a thin `insertMessage(...)` wrapper in `actions/conversation.ts` that always stamps `createdAt` itself, so no call site can omit it by mistake — cheap to add whenever the next `MESSAGE`-inserting story lands.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-selection-du-modele-et-envoi-d-un-message.md`
  summary: When `sendMessage` returns `assistantFailed: true`, no `MESSAGE` row records the failure — `Composer.tsx` shows a transient error banner, but `ConversationHistory` has no durable way to represent "a reply was attempted here and failed." Reloading the page (or navigating away and back) loses all indication that a message went unanswered.
  evidence: Flagged by the Epic 2 retrospective's adversarial pass. Not fixed now: doing this properly needs a design decision (a sentinel assistant row? a status column on `MESSAGE`?) that's more than a mechanical fix, and round 1's primary verification environment has no real `ANTHROPIC_API_KEY` anyway, making the failure path the *common* case during development but plausibly rare for a real tester with a working key. Revisit if testers report confusion about "silently ignored" messages, or when Epic 4 needs richer message states for other reasons.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-suggestion-proactive-de-demarrage.md`
  summary: `_bmad-output/planning-artifacts/ux-designs/ux-GenAI4Consulting-2026-09-11/DESIGN.md` (line 142) still names "Oui, commençons" as the canonical example of `button-ai-primary`, even though the 2026-09-16 renegotiation (`epic-1-retro-item-1`) explicitly deferred that token to Epic 4 and Story 3.3 correctly uses `button-primary` for that exact button instead.
  evidence: Flagged independently by two of Story 3.3's three review lenses. Pre-existing documentation drift: the renegotiation updated `epics.md`'s UX-DR4/UX-DR7 and `sprint-status.yaml` but never touched `DESIGN.md` itself, so a future reader consulting only `DESIGN.md` would reasonably expect this button to be violet. Revisit by updating `DESIGN.md`'s `button-ai-primary`/`ai-suggestion-card` component descriptions to reflect the renegotiated sequencing, ideally alongside Epic 4 when `button-ai-primary` actually gets its first real consumer.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-3-suggestion-proactive-de-demarrage.md`
  summary: A pre-existing React "duplicate key" console warning (on a fixture-seeded conversation id) fires in dev, unrelated to any `key=` usage introduced by Story 3.3.
  evidence: Observed by Story 3.3's verification-gap review lens while manually testing `ProactiveSuggestion` in the browser — present before any interaction with the new component, and confirmed unrelated to `key={conversation.id}` or any other change in this story's diff. Root cause looks like duplicate fixture-conversation ids from `actions/conversation.ts`'s `seedFixturesIfEmpty` under some dev-reload condition, not investigated further here. Revisit if it starts producing visibly wrong conversation-list rendering, not just a console warning.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-generation-des-suggestions-ancrees-a-l-ecriture.md`
  summary: Nothing prevents `propose_livrable_content` from being called more than once in the same conversation — each call inserts a brand-new, fully independent `LIVRABLE` row (by this story's own design: never updates an existing one), so a consultant who prompts the agent to draft "the same document" twice in one conversation ends up with two unrelated-looking cards in `LivrablesPanel`, with no UI cue (or data link surfaced to the UI) that they share an origin conversation.
  evidence: Flagged by Story 4.2's edge-case review lens, confirmed by tracing `actions/livrable.ts`'s `createLivrableWithSuggestions` (always `crypto.randomUUID()`, no existence check) and `LivrableSummary`'s deliberate omission of `conversationId` (FR-10 privacy boundary). Not exercised by round 1's own demonstrated usage narrative (UJ-1/UJ-2 each draft exactly one document per conversation), so left unfixed for now — but any real fix (block a second creation? merge into the existing livrable? group cards by conversation in the UI?) is a product decision, not a mechanical patch. Revisit before/during Story 4.4 (which already extends this same tool for deliberate re-invocation via révision globale) — that story's design should explicitly address whether an *un*prompted second creation should behave differently from a requested révision.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-generation-des-suggestions-ancrees-a-l-ecriture.md`
  summary: `propose_livrable_content` is offered on every single `sendMessage` call, unconditionally (by explicit, deliberate spec design) — any message the model judges tool-worthy costs a full second sequential, non-streamed Anthropic API call, roughly doubling latency and more-than-doubling input token cost for that message, with zero rate limit, cooldown, or heuristic pre-filter, and no user-facing indication a two-call cycle is in progress. **Update (Story 4.5 review, 2026-09-21) — same tradeoff, sharper consequence since Story 4.4.** Before Story 4.4, a false positive on an off-topic message only produced an isolated, harmless extra `LIVRABLE` row (the "called more than once" entry above). Since Story 4.4's `executeTool` now looks up an existing `LIVRABLE` for the conversation and updates it instead of always creating one, that same false positive — if a livrable already exists for this conversation — silently overwrites its `content` via `updateLivrableWithSuggestions` and rejects whatever anchored suggestions were `pending`/`revising`, with no consultant confirmation step anywhere in that path (Option A, spec Story 4.5). This is a consequence of the tool's already-accepted unconditional-offer design, not a gap introduced by Story 4.5's own diff (which makes no code change) — Story 4.5's frozen Boundaries chose Option A (statu quo, documentation only) for this exact risk. **Update (spec-fiabilite-revision-suggestions review, 2026-09-21).** This tradeoff now compounds slightly further: `sendMessage` (`actions/conversation.ts`) reads and injects the existing livrable's full current content into the system prompt on *every* call once one exists for the conversation (not only on a genuine revision turn) — necessary given the tool's already-unconditional offer, but adding token cost on every subsequent turn of a livrable-bearing conversation, not only the one that triggers a real revision. **Update (Epic 4 retrospective, 2026-09-23) — same wasted-cost family, a different call site.** `reworkSuggestion`'s agent call (`skills/rework_suggestion.ts`) can also be a guaranteed-to-be-discarded cost: if a global revision rejects the suggestion being reworked while that call is still in flight, the reformulated result is silently thrown away (the guard added by `spec-fiabilite-revision-suggestions` correctly prevents it from resurrecting the row, but the API call itself still happened and is billed). Same underlying "wasted expensive agent call with no cost control" concern as this entry, just via a different trigger. Not fixed here.
  evidence: Flagged by Story 4.2's edge-case review lens. Confirmed faithful to this story's own frozen Boundaries (unconditional availability was a deliberate choice, following the Story 3.3 `propose_starting_point.ts` precedent of living outside `SKILL_CATALOG`) — not an implementation defect, an accepted architecture tradeoff for this round's prototype. Re-surfaced by Story 4.5's audit of `LIVRABLE.content`'s write paths against FR-23/FR-24 (aucune modification IA sans acceptation explicite du consultant), which traced this exact call path (`actions/conversation.ts`'s `executeTool` → `updateLivrableWithSuggestions`, `actions/livrable.ts`) as the one place `LIVRABLE.content` changes without a distinct, separately-processed suggestion — guided only by the model's judgment, never gated on consultant confirmation. Revisit if real usage shows the false-positive rate or latency/cost is actually a problem: options include a cheap heuristic pre-check before offering the tool, or a per-conversation cooldown once a livrable already exists for it (see the entry above — the two concerns are related).

- source_spec: `_bmad-output/implementation-artifacts/spec-4-3-traitement-d-une-suggestion-ancree.md`
  summary: **Resolved (spec-fiabilite-revision-suggestions, 2026-09-21) — CAP-2, the reopening below.** `reworkSuggestion`'s 3 write-backs to `pending` (success, agent failure, unexpected-error revert) are now each scoped to `WHERE status = 'revising'` (`AND` added to the existing `eq(suggestion.id, suggestionId)`), the exact fix this entry's reopening proposed — same guard-then-write shape as `acceptSuggestion`/`rejectSuggestion`. Verified end-to-end with a throwaway `tsx` script (no test framework in this project, per project-wide constraint): a real `reworkSuggestion` call was raced against a real, concurrent `updateLivrableWithSuggestions` call via a delayed local HTTP stub standing in for the Anthropic API — the suggestion moved to `revising`, was rejected by the concurrent revision (fresh `anchorRef`), and `reworkSuggestion`'s own success-branch write-back (the stricter of its 3 branches, since it also carries new `text`) correctly no-op'd, leaving the row `rejected` with its original text and the revision's fresh anchor — never resurrected as `pending`. The nominal, non-concurrent rework path (no interference) was verified unchanged: `pending` → `revising` → `pending` + new text, exactly as before this fix. `npx tsc --noEmit` and `npx next build --turbopack` both pass clean. `acceptSuggestion`/`rejectSuggestion` confirmed untouched by direct diff. Closing this entry — original reopening note kept below for the record.
  summary_reopened_2026-09-21: **Reopened (Epic 4 retrospective, 2026-09-21) — the 2026-09-18 "Resolved" note below was incomplete.** Originally: `acceptSuggestion` never checked whether `applyAcceptedSuggestion` actually found a matching block before marking a suggestion `accepted`, so a suggestion whose `anchorRef` no longer resolves would silently "succeed" with no real change — reachable only once a livrable's blocks could ever be regenerated (Story 4.4). Story 4.4's `updateLivrableWithSuggestions` transitions every still-`pending`/`revising` anchored suggestion to `rejected` *at the moment it inspects them*, before regenerating blocks with fresh ids — this closes the case where a stale suggestion is caught mid-regeneration. It does **not** close the case where `reworkSuggestion` (`actions/suggestion.ts`) is mid-flight on that same suggestion: its two post-agent-call writes (`status: 'pending'`, success or revert) are unconditional `db.update(...).where(eq(suggestion.id, suggestionId))` calls with no re-check that the row is still `revising` — so a `reworkSuggestion` call that started before a concurrent global revision, and completes after it, silently overwrites the `rejected` status `updateLivrableWithSuggestions` just wrote, resurrecting the suggestion as `pending` with a stale `anchorRef` pointing at a block id that no longer exists (every global revision mints brand-new ids for all blocks, `actions/livrable.ts:295-298`). `acceptSuggestion` on that resurrected row then silently no-ops (`applyAcceptedSuggestion` degrades gracefully) while `SuggestionCard.tsx` reports "Acceptée" — a false-success confirmation. The original "no suggestion status can hold a live, actionable stale anchor after a regeneration" claim is false in this ordering. Original resolution text kept below for the record.
  evidence: Flagged by Story 4.3's edge-case review lens; the 2026-09-18 patch (`inArray(suggestion.status, ['pending', 'revising'])`) was verified directly against the code by the orchestrator, but only for the single-writer case. Reopened by the Epic 4 retrospective's `bmad-review` pass (adversarial lens finding #2 and #3, edge-case-hunter findings #1/#2, verification-gap finding #2 — all three lenses independently converged on the same root cause from different angles) against `actions/suggestion.ts`'s `reworkSuggestion` (lines ~283-363) and `actions/livrable.ts`'s `updateLivrableWithSuggestions` (lines 290-353), confirmed by the orchestrator reading both functions directly. No test exists anywhere in this project (accepted, project-wide constraint) to have caught the regression at the time of the original patch. Fix: scope both of `reworkSuggestion`'s write-backs to `WHERE status = 'revising'` inside a transaction, matching the guard-then-write pattern already used in `acceptSuggestion`/`rejectSuggestion`; no-op (log only) if the row has moved on. Proposed as Epic 4 retrospective action item.
  original_resolution_2026-09-18: "Resolved (Story 4.4 review, 2026-09-18). ... Closing this entry." (see git history of this file for the exact original text)

- source_spec: `_bmad-output/implementation-artifacts/spec-4-2-generation-des-suggestions-ancrees-a-l-ecriture.md`
  summary: **Update (Story 4.4 review, 2026-09-18) — narrowed, not closed.** Originally: nothing prevented `propose_livrable_content` from being called more than once in a conversation, each call creating a fully independent `LIVRABLE` row. Story 4.4's `executeTool` (`actions/conversation.ts`) now looks up an existing `LIVRABLE` by `conversationId` first and updates it instead of creating a second one — but that lookup-then-branch (a `SELECT` followed by either `createLivrableWithSuggestions` or `updateLivrableWithSuggestions`) is not atomic. Two genuinely concurrent `sendMessage` calls on the same conversation (e.g. the composer on `/` and `GlobalRevisionField` on `/livrables/[id]`, two browser tabs, submitting near-simultaneously) could still both observe "no existing livrable" and both create one, or one could act on a stale read.
  evidence: Flagged independently by Story 4.4's blind-hunter and edge-case-hunter review lenses, confirmed by the orchestrator. Narrow window (needs two overlapping requests on the same conversation from different entry points) — not exercised by any current single-request usage narrative. Revisit only if this is ever actually observed, or when/if a unique constraint on `livrable.conversationId` plus an upsert-style write becomes cheap to add. **Update (spec-fiabilite-revision-suggestions review, 2026-09-21) — same family, one more affected read site.** `sendMessage` now also reads `livrable.content` early (before the agent call) to inject it as context; that read and `executeTool`'s own later wholesale overwrite of `content` are not part of one atomic operation, so any write landing on the same livrable during the agent round-trip (an accept/reject, another revision) could in principle be silently discarded by the eventual overwrite. Confirmed pre-existing — `updateLivrableWithSuggestions`'s unconditional overwrite is unchanged by this diff, which only adds an earlier read subject to the same underlying condition. Not fixed here (out of this spec's frozen scope); revisit together with the entry above.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-4-revision-globale.md`
  summary: When `executeTool` has already run `updateLivrableWithSuggestions` (or, on first creation, `createLivrableWithSuggestions`) successfully, a subsequent failure of `sendToAgent`'s second, tool-free `messages.create` call (`skills/buildRequest.ts`) is caught by that function's single outer try/catch and reported identically to a failure that never touched the DB — `sendMessage` returns `{assistantFailed:true, error}` either way. For Story 4.4's révision globale specifically, this means a transient failure on that second call leaves the consultant reading an error that implies "rien n'a changé" while the livrable's content and suggestions have, in fact, already been regenerated (and are immediately visible, since `GlobalRevisionField` calls `router.refresh()` right after) — contradicting this spec's own I/O matrix row for that scenario.
  evidence: Flagged by Story 4.4's verification-gap review lens (its main finding), confirmed by the orchestrator by tracing `buildRequest.ts`'s two-call cycle and `sendMessage`'s error handling. Root cause is entirely pre-existing, unmodified code from Story 4.2 (`sendToAgent`/`sendMessage`'s generic `assistantFailed` shape already has the identical latent gap for the original creation path — a transient failure on the second call after a brand-new `LIVRABLE` is created reports the same generic error with no mention that a document was, in fact, created) — Story 4.4 only reuses that existing ambiguous contract through a new call site, it does not introduce the ambiguity. A real fix needs `sendToAgent`'s return shape to distinguish "no mutation attempted" from "mutation succeeded, reply failed," a cross-cutting change to the one shared assembly point (AD-11) every tool-using caller depends on — well beyond either story's own scope. Revisit if this is ever actually observed, or as part of a dedicated hardening pass on `skills/buildRequest.ts`'s error semantics.

- source_spec: `_bmad-output/implementation-artifacts/spec-changement-de-projet-a-la-volee.md`
  summary: The reopened project-switcher dropdown reads the active project's name from two separate concurrent reads — `getActiveProject()` (used for the top-bar trigger and the "active" marker comparison) and `listProjects()` (used for each row's own label, including the active row's) — that could in principle disagree if the underlying Octopod data changes between the two calls; separately, if `listProjects()` ever omitted the active project's id (a live provider inconsistency), that project would render as an ordinary clickable option instead of the visually distinguished, non-clickable one the spec requires.
  evidence: Flagged by the `bmad-review` edge-case-hunter lens (one normal-confidence, one self-flagged low-confidence), confirmed by the orchestrator to be unreachable with the current static mock provider (`integrations/mock/project-provider.ts`), which returns the same deterministic data from both call sites. Same category as this project's other "revisit with a real (non-mock) provider" deferred entries (Story 1.3's document-sync entries). Revisit once a real `ProjectProvider` replaces the mock.

- source_spec: `_bmad-output/implementation-artifacts/spec-simulation-demarrage-avant-vente.md`
  summary: The avant-vente demo-reset tool (`actions/demo.ts`'s `resetAvantVenteWorkflow`, `components/DemoResetAvantVente.tsx`) — an irreversible, destructive action that wipes a project's CONVERSATION/MESSAGE rows — has no environment gating (feature flag, `NODE_ENV` check, or similar) restricting it to a local/demo deployment. As shipped, it is active in any deployment of this app for any `avant-vente` project.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator: grepped the whole project for `NODE_ENV`/`process.env` — none exists anywhere outside the Anthropic API key read, confirming this app has no environment-separation mechanism at all, for this feature or any other. Not a regression this diff introduces on its own, but **not merely latent either, per a second review pass (2026-09-22)**: every other pre-existing gap of this shape in this project was about a *future* risk conditional on a *future* change (a real provider replacing a mock, a real add-skill mechanism). This one is different — the destructive action already exists, right now, in the current codebase, reachable by anyone with access to any deployment of this app today. Revisit before this app is ever deployed anywhere beyond the small identified pilot group of consultants — do not wait for an incident to trigger the revisit.

- source_spec: `_bmad-output/implementation-artifacts/spec-ai-tint-paragraphe-cible.md`
  summary: `app/livrables/[id]/page.tsx`'s `activeAnchorRefs` computation treats `suggestion.anchorRef !== null` as a proxy for "this is an anchored suggestion", rather than checking `suggestion.type === 'anchored'` explicitly — an unenforced convention (`db/schema.ts` has no `CHECK` constraint tying `type` to `anchorRef` nullability), not a type-level guarantee.
  evidence: Flagged by the `bmad-review` blind-hunter lens (oneshot pass), confirmed by the orchestrator: no code path in this project ever creates a `type: 'global'` suggestion today (Story 4.4's own Boundaries: "SUGGESTION.type: 'global' reste inutilisée") — so a `global` row with a non-null `anchorRef` cannot currently exist, and this proxy is accurate in practice. A proper fix means exposing `suggestion.type` on `SuggestionSummary` (currently only `id`/`anchorRef`/`text`/`status`, `actions/suggestion.ts`) and updating the `listSuggestions` `SELECT` — a small but real extension of a type multiple components already consume (`SuggestionCard.tsx`, `SuggestionsPanel.tsx`), more than the direct correction/deletion this oneshot route's own patch criteria calls for. Revisit once the `global` type actually gets a real writer (Story 4.4's own deferred direction) — at that point this proxy would need fixing for real, not just in theory.

- source_spec: `_bmad-output/implementation-artifacts/spec-position-figee-suggestions-resolues.md`
  summary: `acceptSuggestion`, `rejectSuggestion`, `reworkSuggestion` (`actions/suggestion.ts`) and `getLivrable` (`actions/livrable.ts`) each independently repeat the same "read the livrable, `JSON.parse` its content, check `Array.isArray(content.blocks)`, log and degrade gracefully otherwise" block — this spec adds a fourth near-identical copy (in `rejectSuggestion`) rather than a shared helper.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator by reading all four call sites. Same duplication family already logged at the Epic 2 retrospective (`epic-2-retro-item-12`, still `open` in `sprint-status.yaml`) for a different helper (`seedFixturesIfEmpty`) — this is a second, independent instance of the same underlying pattern (small teams of stories each re-deriving the same defensive read, never noticing a sibling already exists). A `readLivrableBlocks(tx, livrableId)` helper would be the right fix, but extracting it across four call sites is more than the direct correction expected of a single review-round patch. Revisit alongside `epic-2-retro-item-12`, ideally in the same pass.

- source_spec: `_bmad-output/implementation-artifacts/spec-simulation-demarrage-avant-vente.md`
  summary: **Update (Epic 4 retrospective, 2026-09-23) — the reset leaves a permanently orphaned LIVRABLE behind, with two consequences beyond what was already known.** `resetAvantVenteWorkflow` nulls a livrable's `conversationId` rather than deleting it (to avoid re-triggering the fixture-reseed bug this same spec's first round fixed). Two consequences confirmed by three independent `bmad-review` lenses converging on the same root cause: (1) if the demo continues past the reset, `executeTool`'s `conversationId`-keyed existing-livrable lookup (`actions/conversation.ts:556-559`) never finds the orphaned row, so the next `propose_livrable_content` call creates a second, duplicate LIVRABLE for the same project, both listed identically in `LivrablesPanel`; (2) `requestGlobalRevision` (`actions/livrable.ts`) treats `conversationId === null` as "fixture livrable, no origin conversation" — a branch written only for the Story 2.6 seed fixture, now also permanently true for the orphaned real livrable, breaking "révision globale" on it for good, with no recovery path in the UI. The orphaned livrable's `SUGGESTION` rows are handled separately (see the retro's Action Items — deleted on reset going forward).
  evidence: Independently found by `bmad-review`'s adversarial and edge-case-hunter lenses and sharpened by the verification-gap lens (which traced the exact `requestGlobalRevision` breakage), confirmed by the orchestrator reading `actions/demo.ts`, `actions/conversation.ts`'s `executeTool`, and `actions/livrable.ts`'s `requestGlobalRevision` directly. Accepted as a demo-tool limitation rather than fixed: a full fix (actually deleting the livrable) requires first fixing the underlying fixture-reseed condition (`actions/livrable.ts`'s `seedFixturesIfEmpty` reseeding whenever the table is currently empty for a project, rather than tracking "has this project ever had a livrable" persistently) — a bigger, schema-level change judged disproportionate for a demo-only tool. Also note: a livrable's `conversationId: null` now carries two different meanings with nothing to distinguish them (Story 2.6's seed fixture, vs. a real livrable orphaned by a demo reset) — worth resolving together if this is ever revisited.
  evidence_addendum: Also flagged (edge-case-hunter): a genuinely non-transactional race exists between `sendMessage`'s CAP-1 context-injection read and a concurrent `resetAvantVenteWorkflow` deleting the same conversation — and separately, between `app/livrables/[id]/page.tsx`'s two independent reads (`getLivrable`/`listSuggestions`) and a concurrent global revision, which can make the `ai-tint` highlight silently vanish for one render. Both are narrow-window races requiring precise timing during a live demo, same family as this project's other already-deferred concurrency findings — not fixed here.

- source_spec: `_bmad-output/implementation-artifacts/spec-changement-de-projet-a-la-volee.md`
  summary: **Update (Epic 4 retrospective, 2026-09-23).** `app/page.tsx` now fetches `listProjects()` unconditionally on every page load, even though the result is only used if the consultant actually reopens the project dropdown (a rare interaction, unlike the original Story 1.2 case where it was the only way to get a project at all).
  evidence: Flagged by the `bmad-review` adversarial lens. Harmless with the current static mock provider; with a real (possibly slow or rate-limited) `ProjectProvider`, this adds latency/cost to every single page load for a feature most sessions never use. Same "revisit with a real provider" category as this spec's other deferred entries above. Not fixed here.

- source_spec: `_bmad-output/implementation-artifacts/spec-refactor-conversation-message-dedup.md`
  summary: `actions/seed-if-empty.ts`'s `seedIfEmpty(hasExistingRows, insertFixtures)` types `insertFixtures` as `() => void`, which TypeScript's lax function-return-type variance also accepts a `() => Promise<void>` for — a future 4th caller passing an async insert closure would compile cleanly but silently fail to complete inside the surrounding synchronous `db.transaction`.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator reading the helper's signature. Not reachable today — all 3 current callers (`actions/conversation.ts`, `actions/skill.ts`, `actions/livrable.ts`) pass synchronous closures. Revisit only if a future caller's fixture-insertion logic needs to be async.

- source_spec: `_bmad-output/implementation-artifacts/spec-ordre-chargement-skills.md`
  summary: `project_skill.position` (new column, epic-2-retro-item-14) has no schema-level guarantee of ever being non-null or unique per project — only `actions/skill.ts`'s `seedFixturesIfEmpty` (the sole writer today) sets it correctly, by convention rather than constraint.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator: no insert path exists today that could produce a null or duplicate `position` (the real "add a skill" feature — the only other writer this table will ever need — is explicitly deferred, PRD OQ-6), so a `CHECK`/uniqueness constraint would guard a currently unreachable code path. Revisit once that feature is actually built: add `NOT NULL` (would need a real backfill-compatible migration path, same shape as this spec's own) and a unique index on `(project_id, position)` at that point.
  evidence_addendum: Also flagged (same lens): the new `.orderBy(asc(projectSkill.position))` has no supporting index (`EXPLAIN QUERY PLAN` confirms a `TEMP B-TREE` sort) and the migration's backfill uses an O(n²) correlated subquery — both negligible at this table's actual scale (4 rows total today, a handful per project at most) and not worth the schema churn now.

- source_spec: `_bmad-output/implementation-artifacts/spec-indicateur-echec-durable.md`
  summary: `MessageSummary.assistantFailed`/`assistantErrorText` are plain fields with no type-level tie to `role` — nothing stops a future edit from reading or setting them on an `assistant`-role row, even though every comment around this feature insists they're only ever meaningful on `role:'user'` rows.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator: no current write path sets these columns on anything but the `role:'user'` row `sendMessage` just inserted (`message.id` is the primary key, so `markAssistantFailed`'s `UPDATE ... WHERE id = ?` cannot target any other row), so nothing reachable today violates the invariant. A real fix means a discriminated union replacing `MessageSummary`'s single flat shape — a refactor of an already-established type and every consumer of it (`ConversationHistory.tsx`'s role-based branching), well beyond a direct correction for a currently unreachable risk. Revisit only if a future change actually needs to write these columns from somewhere other than `sendMessage`.

- source_spec: `_bmad-output/implementation-artifacts/spec-wrapper-insert-message.md`
  summary: `MESSAGE.createdAt` has no secondary sort tiebreaker (e.g. `id`) — two messages landing in the same millisecond (plausible under concurrent `sendMessage` calls on the same conversation, e.g. a double-submit or two tabs) would sort nondeterministically under `actions/message.ts`'s/`actions/conversation.ts`'s plain `.orderBy(message.createdAt)`.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator: pre-existing, unaffected by this spec — `new Date().toISOString()` was already computed independently by each insert site before `insertMessage` centralized the call, so this collision risk is not introduced or worsened here, just relocated into the shared wrapper. Revisit only if message ordering under real concurrent load ever becomes an observed problem (this project's actual usage pattern — one consultant per conversation — makes same-millisecond concurrent sends unlikely today).

- source_spec: `_bmad-output/implementation-artifacts/spec-indicateur-echec-durable.md`
  summary: Right after a failed `sendMessage`, the same failure now shows twice at once — `Composer.tsx`'s transient `assistantError` (next to the composer, cleared only on the next submit) and `ConversationHistory.tsx`'s new durable indicator (under the failed message, sourced from `MESSAGE.assistantFailed`/`assistantErrorText`) both render simultaneously once `router.refresh()` completes.
  evidence: Flagged by the `bmad-review` blind-hunter lens (tour 2), confirmed by the orchestrator: `router.refresh()` re-renders the Server Component tree but does not remount `Composer` (no `key` change on a same-conversation send), so its local `assistantError` state persists alongside the newly-rendered durable indicator. Not incorrect (no information is lost or wrong), just redundant. A clean fix (e.g. suppressing the transient toast once the durable one exists, or removing `assistantError` from `Composer` entirely now that the durable indicator covers the same ground) is a real design decision beyond a direct correction, and risks weakening the composer's own immediate-feedback path if `router.refresh()` is ever slow or fails. Revisit if this redundancy is ever reported as confusing in practice.
  evidence_addendum: Also flagged (same lens): `markAssistantFailed`'s two failure paths (a 0-row `UPDATE`, or the `UPDATE` itself throwing) only `console.error` — nothing surfaces to the consultant that the durable copy of an already-shown transient error failed to persist. Deliberate, matching this spec's own Boundaries ("pas de mécanisme de nouvelle tentative... indicateur visuel durable" — no scope for a secondary failure-reporting UI) and the same log-only precedent `actions/suggestion.ts`'s `reworkSuggestion` already established for a comparable concurrent-write race. Revisit only alongside a broader look at surfacing background write failures generally, not specific to this one column.
  evidence_addendum_2: Also flagged (verification-gap lens, tour 2): the entire write→read→render path for this feature (3 `markAssistantFailed` call sites, `getActiveConversation`'s read, `ConversationHistory`'s render, and the fixture-seeding false-positive guarantee) has no automated regression coverage — only a disposable `tsx` script run once by hand and deleted. A future edit could silently drop one of the 3 call sites (reintroducing the exact bug this item closes) with nothing in the repo's build/typecheck path catching it. Same disposition as every other verification-gap finding in this project: zero test infrastructure exists anywhere (no runner, no library in `package.json`), so closing this within one item's scope would mean introducing project-wide test infra, disproportionate here.

- source_spec: none
  summary: Split off `spec-mode-demo-scripte.md` (mode démo scripté) at the user's request, to keep that spec's token budget within the SCOPE STANDARD's 1600-token target — a reference document appearing in the Contexte panel at the moment the demo script triggers the `propose_livrable_content` tool call, to visually suggest the agent is drawing on real project context.
  evidence: Not a defect — a deliberate scope narrowing. `spec-mode-demo-scripte.md` keeps the chat script, the tool-call flow (real LIVRABLE/SUGGESTION creation), the Stepper's per-step proactive suggestions, and `DEMO_MODE` detection. This document beat is a secondary embellishment on top of that core goal, cleanly separable since it only hooks into the same tool-call trigger point already built by the main spec. Revisit as its own `bmad-build` pass once the main demo-mode spec has shipped and been rehearsed — at that point `actions/message.ts`'s `executeTool` already has the right hook point (right after a successful `createLivrableWithSuggestions`/`updateLivrableWithSuggestions`, gated on `isDemoModeActive()`) to add the document-seeding call.

- source_spec: `_bmad-output/implementation-artifacts/spec-mode-demo-scripte.md`
  summary: `skills/demoScript.ts`'s keyword matching (`matchDemoChatEntry`) is plain case-insensitive substring matching with no accent/diacritic normalization or word-boundary check — a message typed without accents (e.g. "references" instead of "références"), or a keyword embedded inside an unrelated word, could silently fall through to the generic fallback instead of the intended scripted reply.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator reading `matchDemoChatEntry`/`matchDemoStepSuggestion`. Low practical risk today: the presenter following their own prepared script types French text on their own keyboard, where accents are readily available, unlike a hypothetical non-native or copy-pasted input. Revisit with a normalization pass (e.g. NFD + strip diacritics before comparing) if a real rehearsal shows this actually causing a mismatch.
  evidence_addendum: Also flagged (same lens): a duplicate-livrable-creation concern was checked directly by the orchestrator and found false — `actions/message.ts`'s `executeTool` already looks up an existing LIVRABLE by `conversationId` and updates it instead of creating a second one (Story 4.4's existing guard), unaffected by whether the tool input came from a real model or this demo script.

- source_spec: `_bmad-output/implementation-artifacts/spec-mode-demo-scripte.md`
  summary: **Accepted trade-off (Checkpoint 1 decision, confirmed real by review).** `proj-audit-mission` conversations get only the generic fallback by design (no `projectId` threaded into the matching functions, per the user's own Checkpoint 1 choice) — but the RFP script's keywords are generic enough French words (`expert`, `référence`) that a mission-project conversation could plausibly use them in its own unrelated context and receive Acme-Corp-themed canned content instead of the intended fallback.
  evidence: Flagged by the `bmad-review` edge-case-hunter lens as a claim check against the spec's own Checkpoint 1 decision text, confirmed real by the orchestrator (e.g. "il faudrait un expert-comptable" in an audit conversation would match the `expert` keyword). Already a known, named, and accepted cost of the decision to avoid a `sendToAgent` signature change (Boundaries: "aucun changement de signature") — fixing it properly means passing `projectId` through, which the user explicitly chose not to do. Revisit only if this is actually observed confusing a real audience; the presenter's own prepared path (`proj-acme-rfp`) never triggers it.

- source_spec: `_bmad-output/implementation-artifacts/spec-mode-demo-scripte.md`
  summary: No warning or log if `DEMO_MODE=true` is left set alongside a valid `ANTHROPIC_API_KEY` in a real deployment — `isDemoModeActive()` simply takes precedence silently, so a real user could receive canned replies with no trace in logs explaining why.
  evidence: Flagged by the `bmad-review` blind-hunter lens. A config-hygiene nice-to-have, not a functional defect — the Boundaries only require that a *missing* key fail visibly, which is unaffected. Revisit alongside a broader look at startup-time configuration validation/warnings, if this project ever adds one; not worth a one-off check for this single variable.
  evidence_addendum: Also flagged (edge-case-hunter): no guard for `tool`/`executeTool` being supplied one without the other. Rejected by the orchestrator as an unreachable path — every current caller of `sendToAgent` passes both or neither, matching this project's established practice of not guarding code paths not shown to be reachable.

- source_spec: `_bmad-output/implementation-artifacts/spec-mode-demo-scripte.md`
  summary: `skills/demoScript.ts` has no automated regression test, despite the code's own comments documenting two real bugs already found and fixed during this spec's own review (a keyword-ordering bug that silently blocked the tool-call trigger, and a missing third-caller discrimination that corrupted suggestion rework text) — a future edit to the script could silently reintroduce either class of bug.
  evidence: Flagged by both the blind-hunter and verification-gap lenses. Same disposition as every other verification-gap finding in this project: zero test infrastructure exists anywhere (no runner, no library in `package.json`), so closing this within one item's scope would mean introducing project-wide test infra, disproportionate here. The manual `tsx` verification scripts used during this spec's implementation and review are the accepted substitute, per this project's established practice — not preserved as a committed file.

- source_spec: `_bmad-output/implementation-artifacts/spec-toggle-mode-demo-ui.md`
  summary: **Broader implication of a fix this spec had to make.** `app/livrables/[id]/page.tsx` was missing `export const dynamic = 'force-dynamic'` — its own comment incorrectly claimed the dynamic `[id]` segment alone was enough to keep it off Next's Full Route Cache, which this spec's own review disproved by reading this Next version's bundled docs directly (`node_modules/next/dist/docs/01-app/04-glossary.md`'s "Request-time APIs" list never includes `params`; `use-router.md` states `router.refresh()` "does not invalidate the server-side cache"). Fixed here because the demo-mode banner/border needed it (this spec's own Boundary: visible on every page). But the same missing directive means every *other* mutation on this page — accepting/rejecting/reworking a suggestion, a global revision — could, in a production `next build`/`next start` deployment (never observable in `next dev`, which always renders on demand), have been serving a stale cached render after a `router.refresh()`, independent of demo mode entirely.
  evidence: Flagged by the `bmad-review` verification-gap lens, confirmed by the orchestrator by reading the exact Next.js documentation lines cited above directly. This project has never called `revalidatePath`/`revalidateTag` anywhere (confirmed by repo-wide search) — the fix applied here (`force-dynamic`) closes the gap for this one route as a side effect of fixing this spec's own requirement, but the same audit (does every route needing per-request freshness actually declare it?) was not performed for the rest of the app. `/` already has `force-dynamic` (Story 1.2). No other page routes exist today. Revisit only if a new page route is ever added that reads mutable data without either `force-dynamic` or a Request-time API.

- source_spec: `_bmad-output/implementation-artifacts/spec-toggle-mode-demo-ui.md`
  summary: `DemoModeToggle.tsx`'s button label always shows the current state ("Mode démo activé"/"désactivé") rather than the action a click performs, and relies only on a border/fill color change (no icon) for its toggle affordance — could read as a status indicator rather than a clickable control before hovering it.
  evidence: Flagged by the `bmad-review` blind-hunter lens. A genuine UX judgment call (exact wording/icon), not a functional defect — the button already has correct `aria-pressed` semantics and hover/cursor affordances. No established convention exists yet in this codebase for how toggle-style controls should read (this is the first one). Revisit if this is ever reported as confusing in practice, or alongside a broader look at this app's toggle/switch conventions once more than one exists.
- source_spec: `_bmad-output/implementation-artifacts/spec-toggle-mode-demo-ui.md`
  summary: `app/layout.tsx` and `app/page.tsx` each independently call `getDemoModeActive()` (two separate `db` reads of the same singleton row) rather than sharing one read — in principle the two could disagree within a single render if a write landed between them.
  evidence: Flagged by the edge-case-hunter lens, confirmed by the orchestrator to be an extremely narrow, effectively unreachable race in this single-process, single-user local SQLite app: `setDemoModeActive` only ever runs from a separate client-triggered request, and the two reads happen back-to-back with no `await` boundary a concurrent write could land in between (matching the reasoning already accepted for this project's other narrow-race deferred entries, e.g. Story 2.1's active-conversation refresh). Revisit only alongside a broader request-level caching mechanism (e.g. React's `cache()`) if this project ever needs one for other reasons.

- source_spec: `_bmad-output/implementation-artifacts/spec-demo-ajout-skill.md`
  summary: A `PROJECT_SKILL` row added via the demo-mode mock "Ajouter une skill" carries no marker distinguishing it from any other row once inserted — `ProjectSkillSummary`/`listProjectSkills` have no provenance field, and the row persists even after demo mode is later turned off.
  evidence: Flagged by the `bmad-review` blind-hunter lens. Same accepted trade-off as every other row this demo mode creates (livrables, suggestions from the scripted chat) — consistent with the user's own stated priority at this stage ("le produit est actuellement construit avant tout pour faire des démos"). Revisit only if this project moves toward a real deployment where distinguishing demo-created data from real consultant work actually matters.
- source_spec: `_bmad-output/implementation-artifacts/spec-demo-ajout-skill.md`
  summary: No focus management or persistent success feedback after adding a skill — once `router.refresh()` lands, the clicked button disappears from the list with only that shrinking list as a signal, and keyboard focus is left on `<body>`.
  evidence: Flagged by the `bmad-review` blind-hunter lens, confirmed by the orchestrator to be a codebase-wide characteristic, not specific to this feature — every other "success -> `router.refresh()`" flow in this app (`ProjectSelector`, `Composer`, `GlobalRevisionField`, etc.) has the exact same property, none does dedicated focus management either. Revisit as a cross-cutting accessibility pass across all of these flows together, not as a one-off fix here.
