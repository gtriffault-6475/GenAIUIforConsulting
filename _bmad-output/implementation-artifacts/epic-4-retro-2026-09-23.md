---
epic: 4
date: 2026-09-23
verdict: accepted-with-open-items
criteria: declared
headless: false
---

# Rétrospective (suivi) — Epic 4 : Éditeur assisté par IA

## Résumé de l'épic

- **Périmètre de ce passage :** tout ce qui a été livré depuis la précédente rétrospective (`epic-4-retro-2026-09-21.md`, verdict `accepted-with-open-items`) : les 4 items de suivi de cette rétro (2 HIGH, 2 MEDIUM) plus 2 nouvelles fonctionnalités de démo demandées par Gautier entre-temps (changement de projet à la volée, simulation de démarrage d'une avant-vente). L'épic lui-même (Stories 4.1-4.5) reste inchangé -- ce passage évalue le travail de suivi, pas l'épic d'origine.
- **Plage de diff :** `381eb49..a01e4ca` (5 commits, aucun merge).
- **Commits :**
  1. `7f1e151` -- fix: ground global revision in real content, guard reworkSuggestion races (finding #1+#2 HIGH de la rétro du 21/09)
  2. `fe4160e` -- feat: switch active project on the fly from the top bar (demande démo)
  3. `bf4b276` -- feat: reset an avant-vente project's workflow for demo replay (demande démo)
  4. `b07a5d8` -- feat: highlight the paragraph an anchored suggestion targets (finding #4 MEDIUM de la rétro du 21/09)
  5. `a01e4ca` -- fix: freeze a resolved suggestion's paragraph position permanently (finding #3 MEDIUM de la rétro du 21/09)
- **Stories :** aucune nouvelle story d'epic -- tout ce travail vit dans des specs `bmad-build` autonomes (`spec-fiabilite-revision-suggestions.md`, `spec-changement-de-projet-a-la-volee.md`, `spec-simulation-demarrage-avant-vente.md`, `spec-ai-tint-paragraphe-cible.md`, `spec-position-figee-suggestions-resolues.md`), chacune déjà passée par sa propre Reviewer Gate à 3 lentilles (une des cinq, sur la révision globale, a nécessité 2 tours ; une autre, sur la réinitialisation avant-vente, a nécessité 3 tours -- voir chaque spec pour le détail). `pending_stories` de l'épic lui-même reste vide (`sprint_status.py detect-epic --epic 4`).
- **Contexte d'exécution :** session interactive continue, cinq changements dispatchés un par un par l'orchestrateur (subagent d'implémentation + Reviewer Gate indépendante à chaque fois), jamais en parallèle. Un incident de process noté et corrigé en cours de route : un subagent d'implémentation (réinitialisation avant-vente) a dépassé son mandat en déclenchant lui-même les 3 lentilles de revue au lieu de laisser l'orchestrateur s'en charger -- recadré, mémoire projet mise à jour ([[feedback-reviewer-gate-discipline]]).
- **Rétrospective précédente :** `epic-4-retro-2026-09-21.md`. Voir Previous-retro follow-through ci-dessous.
- **Logs de session :** aucun export séparé ; connaissance directe (même session que tout le travail de suivi).

## Constats

`bmad-review` (adversarial, edge-case-hunter, verification-gap) a été lancé sur le diff complet du passage (`381eb49..a01e4ca`, 2444 lignes), avec instruction explicite de porter attention aux interactions ENTRE les 5 changements -- chacun déjà passé par sa propre Reviewer Gate isolément, mais jamais vus ensemble.

### Findings — lentilles `bmad-review` (vérifiés)

**#1 — [HIGH, fix now partiel + defer résiduel] Le livrable orphelin après une réinitialisation avant-vente.** Convergence des 3 lentilles (adversarial #1/#2, edge-case-hunter #1, verification-gap), vérifiée par l'orchestrateur dans le code. `resetAvantVenteWorkflow` (`actions/demo.ts`) ne supprime jamais le livrable existant -- seulement `conversationId` mis à `null`, pour éviter de réintroduire le bug de re-seed automatique du tour précédent (`seedFixturesIfEmpty` re-remplit dès que la table `livrable` est vide pour ce projet). Conséquences vérifiées : (a) si la démo continue après le reset, `executeTool` (`actions/conversation.ts:556-559`) cherche un livrable par `conversationId` -- la nouvelle conversation n'en trouve aucun, donc un **deuxième livrable dupliqué** se crée ; (b) `requestGlobalRevision` (`actions/livrable.ts`) traite `conversationId === null` comme "livrable fixture, pas de conversation d'origine" -- une branche prévue uniquement pour le seed Story 2.6, maintenant vraie en permanence pour un vrai livrable qui vient de perdre sa conversation : **révision globale cassée définitivement** dessus ; (c) les `SUGGESTION` de ce livrable ne sont jamais touchées -- restent `pending`/`revising` et pleinement actionnables (acceptables/rejetables/retravaillables) sur un document censé être remis à zéro.
Source vérifiée : `actions/demo.ts` (transaction complète), `actions/conversation.ts:556-559`, `actions/livrable.ts` (`requestGlobalRevision`, branche `conversationId === null`).

**#2 — [Low, fix now] La réinitialisation ne vérifie pas que le projet ciblé est toujours le projet actif.** Trouvé par edge-case-hunter : avant l'ajout du changement de projet à la volée (ce même passage), aucun code ne pouvait jamais rendre un projet affiché "non actif" -- un seul projet pouvait jamais être sélectionné. Le nouveau `ProjectSelector` rend cette situation atteignable (un onglet resté ouvert sur l'avant-vente pendant qu'un autre onglet change le projet actif). `resetAvantVenteWorkflow` ne vérifie que `project.type === 'avant-vente'`, jamais que `projectId` est bien le projet actif courant.
Source vérifiée : `actions/demo.ts` (garde de type, aucune vérification d'activité), `components/ProjectSelector.tsx` (`selectProject` peut changer `activeProjectId` indépendamment).

**Findings vérifiés faux, hors périmètre, ou différés, par souci de complétude :**
- Adversarial #3 (état résiduel `DemoResetAvantVente` -- erreur/`busyRef` -- fuyant lors d'un changement direct entre deux projets avant-vente) : **Faux/non atteignable**. Un seul projet avant-vente existe dans les fixtures actuelles (`proj-acme-rfp`) -- passer d'un projet à l'autre bascule toujours avant-vente <-> mission, ce qui démonte/remonte `DemoResetAvantVente` entièrement (rendu conditionnel), réinitialisant son état par construction React. Deviendrait pertinent si un second projet avant-vente était ajouté un jour.
- Adversarial #4 (lecture CAP-1 + suppression concurrente de `resetAvantVenteWorkflow`, fenêtre de course élargie) : réel mais nécessite un timing serré pendant une démo live (cliquer envoyer et réinitialiser presque simultanément) -- même famille que les autres races déjà différées dans ce projet. Loggé dans `deferred-work.md`.
- Adversarial #5 (dépendance implicite non documentée entre `activeAnchorRefs` et la garde CAP-2 de `reworkSuggestion`) : réel, corrigé -- commentaire de traçabilité ajouté (voir Action items).
- Adversarial #6 (regroupement méta de deux findings déjà connus/différés sous un même angle "métadonnée projet mouvante") : pas un nouveau finding, commentaire seulement.
- Adversarial #7 (boucle par-suggestion vs `UPDATE` en masse dans `updateLivrableWithSuggestions`) : réel mais sans conséquence mesurable à cette échelle (quelques suggestions par livrable) -- rejeté.
- Adversarial #8 (le `¶N` figé de `resolvedPosition` ne signale pas qu'il décrit un état passé du document) : nuance UX réelle mais jamais demandée par aucun des deux specs concernés -- hors périmètre.
- Adversarial #9 (coût gaspillé d'un `reworkSuggestion` voué à l'échec non relié à l'entrée `deferred-work.md` existante sur le coût de `propose_livrable_content`) : corrigé -- référence croisée ajoutée.
- Adversarial #10 (`listProjects()` payé à chaque chargement de page pour une interaction rare) : réel, loggé en complément de l'entrée `deferred-work.md` déjà existante sur ce spec.
- Adversarial #11 (`conversationId: null` porte deux significations distinctes -- fixture vs. livrable orphelin de reset -- sans rien pour les distinguer) : réel, loggé aux côtés du finding #1 dans `deferred-work.md`.
- Edge-case-hunter #2 (lecture non-transactionnelle `getLivrable`+`listSuggestions`, tint pouvant disparaître silencieusement si une révision globale commit entre les deux) : réel mais fenêtre de course étroite, même famille que les races déjà différées. Loggé dans `deferred-work.md`.

### Vues agrégées

**God-file -- `actions/conversation.ts` et `actions/suggestion.ts`, croissance non traitée pour la 3e fois.** `actions/conversation.ts` : 458 lignes (rétro Epic 2) -> 695 (rétro Epic 4 du 21/09) -> **758 lignes aujourd'hui** (+65% depuis Epic 2). `actions/suggestion.ts` : 392 -> **538 lignes** sur ce seul passage (+37%). `epic-2-retro-item-13` ("scinder actions/conversation.ts si le fichier continue de grossir") reste `open` dans `sprint-status.yaml` depuis la rétro Epic 2 -- deux rétrospectives l'ont maintenant signalé sans effet, et le fichier a continué de grossir sur ce passage aussi (`actions/conversation.ts` +63 lignes nettes, `git_evidence.py`).

**Duplication -- 4e copie du motif "lire le livrable, parser le JSON, vérifier `Array.isArray(blocks)`" (déjà loguée pendant ce passage).** `epic-2-retro-item-12` (même famille, sur `seedFixturesIfEmpty`) reste également `open`. `actions/suggestion.ts::rejectSuggestion` (spec-position-figee) et `actions/livrable.ts::updateLivrableWithSuggestions` (même spec, tour 2) en ajoutent chacune une nouvelle instance -- déjà noté dans `deferred-work.md` en cours de revue, reconfirmé ici comme un pattern qui continue de se répéter à travers les rétros sans jamais être traité.

**Pattern convergence (positif) -- tous les nouveaux specs restent en français, malgré `config.yaml` qui déclare `English`.** Cohérent avec la convention déjà établie et déjà signalée comme point de vigilance à la rétro Epic 2 (`epic-2-context.md` en anglais, corrigé à l'époque) -- aucune régression sur ce point depuis.

**Spec-to-implementation -- les 2 items HIGH de la rétro du 21/09 sont bien couverts, mais leur propre correctif a nécessité 2 tours de revue avant d'être complet.** `spec-fiabilite-revision-suggestions.md` a d'abord manqué de vérifier si l'écriture gardée de `reworkSuggestion` remontait une erreur correcte en cas de no-op silencieux -- corrigé en revue avant commit (voir cette spec's Review Triage Log). Les findings CAP-1/CAP-2 de la rétro du 21/09 sont bien fermés, vérifiés indépendamment par l'orchestrateur contre la base réelle à chaque tour.

## Behavior verification

Chaque changement de ce passage a été vérifié indépendamment par l'orchestrateur contre `db/local.db` réelle (sauvegardée/restaurée à chaque fois), jamais seulement le rapport du subagent implémenteur : la course `reworkSuggestion`/révision globale (script HTTP différé simulant l'agent), le changement de projet (relecture directe du code, pas de rendu réel possible), la réinitialisation avant-vente (deux bugs FK réels trouvés et corrigés via ce mécanisme), le fond `ai-tint` (relecture de code uniquement, pas de rendu réel possible), et `resolvedPosition` (accepté/rejeté/auto-rejeté, survit à une révision globale répétée -- vérifié explicitement). Aucun rendu réel dans un navigateur n'a été possible sur l'ensemble de ce passage : le serveur de dev refuse de démarrer dans cette session (restriction d'environnement, session non-interactive à l'origine) -- risque résiduel déjà divulgué à chaque spec concernée, non levé ici.

## Previous-retro follow-through

| Item | Statut proposé | Preuve |
|---|---|---|
| Finding #1 (HIGH, révision globale sans contenu réel) | **done** | `7f1e151`, vérifié indépendamment par l'orchestrateur |
| Finding #2 (HIGH, résurrection `reworkSuggestion`) | **done** | `7f1e151`, corrigé en 2 tours, vérifié indépendamment |
| Finding #3 (MEDIUM, régénération d'id de bloc) | **done** | `a01e4ca`, couverture étendue à l'auto-rejet en 2e tour, vérifié indépendamment |
| Finding #4 (MEDIUM, `ai-tint` sur le paragraphe) | **done** | `b07a5d8` |
| `epic-2-retro-item-12` (duplication `seedFixturesIfEmpty`) | **open, aggravé** | Nouvelle instance du même pattern ajoutée sur ce passage (voir Vues agrégées) |
| `epic-2-retro-item-13` (scinder `actions/conversation.ts`) | **open, aggravé** | Fichier passé de 695 à 758 lignes sur ce seul passage |

## Action items

1. **[Fix now] Supprimer les `SUGGESTION` du livrable orphelin lors d'une réinitialisation avant-vente (Option 1, validée par Gautier).** Finding #1, partiel -- referme le point le plus grave (contenu IA actionnable survivant à un reset), laisse le livrable lui-même en place (limite acceptée, voir ci-dessous) pour ne pas rouvrir le bug de re-seed. Owner : dev loop.
2. **[Fix now] `resetAvantVenteWorkflow` refuse si `projectId` n'est plus le projet actif courant.** Finding #2. Owner : dev loop.
3. **[Fix now, trivial] Commentaire de traçabilité dans `app/livrables/[id]/page.tsx` documentant la dépendance de `activeAnchorRefs` envers la garde `reworkSuggestion`/CAP-2.** Adversarial #5. Owner : dev loop.
4. **[Fix now, trivial] Référence croisée dans `deferred-work.md` entre le coût gaspillé d'un `reworkSuggestion` voué à l'échec et l'entrée existante sur le coût de `propose_livrable_content`.** Adversarial #9. Owner : dev loop.
5. **[Limite acceptée, documentée] Le livrable orphelin après un reset avant-vente reste en place** -- carte dupliquée possible si la démo continue, révision globale cassée dessus. Accepté comme limite de l'Option 1 plutôt que rouvrir le mécanisme de re-seed (Option 2, jugée disproportionnée pour un outil de démo). Loggé dans `deferred-work.md`.
6. **[Process, non traité sur ce passage -- 3e signalement] `epic-2-retro-item-13` (scinder `actions/conversation.ts`) et `epic-2-retro-item-12` (extraire `seedFixturesIfEmpty`/motif de lecture livrable partagé) restent `open`, tous deux aggravés sur ce passage.** Owner : product (priorisation) -- deux rétrospectives consécutives n'ont eu aucun effet.

## Acceptance verdict

**Verdict : `accepted-with-open-items`.** Critères : `declared` (les 4 findings de la rétro du 21/09 servent de critères pour ce passage de suivi).

`pending_stories` vide -- aucune story d'epic inachevée. Les 3 findings HIGH/MEDIUM de la rétro précédente (fiabilité révision globale, résurrection `reworkSuggestion`, régénération d'id de bloc) sont solidement fermés et vérifiés indépendamment par l'orchestrateur contre la base réelle à chaque tour. Mais l'une des deux nouvelles fonctionnalités de démo (réinitialisation avant-vente) portait un défaut réel et sérieux pour son propre objectif ("table rase" pour la démo) -- atténué mais pas entièrement fermé par la décision de Gautier (Option 1). Ce n'est pas bloquant (aucune story d'epic affectée, outil de démo non consultant-facing), mais c'est un finding réel et nommé qui reste ouvert -- exactement la définition d'`accepted-with-open-items`.

## Open questions

Aucune question ouverte restante -- verdict et routage des findings tranchés par Gautier ci-dessus.
